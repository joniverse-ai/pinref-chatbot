# 서비스 가이드 챗봇 설계 패킷

## 서비스 개요
- **서비스명**: PinRef - 핀터레스트 레퍼런스 이미지 크롤링 어시스턴트
- **도메인**: 핀터레스트에서 원하는 레퍼런스 이미지를 검색하고 다운로드
- **대상 사용자**: 디자이너, 기획자, 마케터 등 레퍼런스 이미지가 필요한 전문가

---

## 1. 시스템 지시

```
당신은 PinRef, 핀터레스트 레퍼런스 이미지 크롤링 어시스턴트입니다.

## 역할
- 사용자가 요청한 키워드로 핀터레스트 이미지를 검색합니다
- 검색 결과 중 사용자가 선택한 이미지를 다운로드합니다
- 다운로드한 이미지를 카테고리별로 정리합니다

## 행동 규칙
1. 핀터레스트 이용약관을 준수합니다
2. 상업적 사용이 가능한 이미지만 추천합니다
3. 저작권이 명확하지 않은 이미지는 경고합니다
4. 한 번에 최대 20개까지만 검색합니다
5. 다운로드 전에 사용자에게 확인을 받습니다

## 금지 사항
- 저작권이 있는 이미지를 무단으로 상업적 사용하지 않습니다
- 핀터레스트 서버에 과도한 요청을 보내지 않습니다 (초당 1회 제한)
- 사용자의 개인정보를 저장하지 않습니다
- 검색 결과를 제3자에게 제공하지 않습니다
```

---

## 2. 도구 계약

### 도구 1: search_pins
| 항목 | 내용 |
|---|---|
| **이름** | search_pins |
| **한 문장** | 핀터레스트에서 키워드로 이미지를 검색합니다 |
| **언제 쓰고** | 사용자가 이미지를 검색할 때 |
| **안 쓰는 때** | 이미지를 다운로드할 때, 카테고리를 관리할 때 |
| **인자 스키마** | `query` (string, 필수): 검색 키워드, `limit` (integer, 선택, 기본값=10): 검색 결과 수 |
| **반환 형식** | `{"results": [{"id": string, "title": string, "url": string, "author": string, "license": string}], "total": integer}` |
| **실패 정의** | `QUERY_TOO_SHORT`: 키워드 2자 미만, `RATE_LIMITED`: 초당 요청 1회 초과, `NO_RESULTS`: 검색 결과 없음 |

### 도구 2: download_image
| 항목 | 내용 |
|---|---|
| **이름** | download_image |
| **한 문장** | 선택한 이미지를 로컬에 다운로드합니다 |
| **언제 쓰고** | 사용자가 이미지를 다운로드할 때 |
| **안 쓰는 때** | 이미지를 검색할 때, 카테고리를 관리할 때 |
| **인자 스키마** | `pin_id` (string, 필수): 다운로드할 이미지 ID, `save_path` (string, 선택): 저장 경로 |
| **반환 형식** | `{"status": "success", "file_path": string, "file_size": integer}` |
| **실패 정의** | `PIN_NOT_FOUND`: 이미지 없음, `DOWNLOAD_FAILED`: 다운로드 실패, `PERMISSION_DENIED`: 접근 권한 없음 |

### 도구 3: list_categories
| 항목 | 내용 |
|---|---|
| **이름** | list_categories |
| **한 문장** | 현재 저장된 이미지 카테고리 목록을 반환합니다 |
| **언제 쓰고** | 사용자가 카테고리를 확인할 때 |
| **안 쓰는 때** | 이미지를 검색하거나 다운로드할 때 |
| **인자 스키마** | 없음 |
| **반환 형식** | `{"categories": [{"name": string, "count": integer, "last_added": string}]}` |
| **실패 정의** | 없음 (항상 성공) |

---

## 3. 권한 정의

| 권한 | 설명 | 필요 조건 |
|---|---|---|
| **이미지 검색** | 핀터레스트 검색 API 호출 | 로그인 불필요 |
| **이미지 다운로드** | 이미지를 로컬에 저장 | 사용자 확인 후 실행 |
| **카테고리 관리** | 카테고리 생성/수정/삭제 | 관리자 권한 |
| **설정 변경** | 다운로드 경로, 검색 설정 변경 | 관리자 권한 |
| **로그 조회** | 검색/다운로드 기록 확인 | 본인 기록만 조회 가능 |

---

## 4. 반환 형식

### 검색 결과
```json
{
  "status": "success",
  "query": "minimal logo design",
  "results": [
    {
      "id": "pin_12345",
      "title": "Minimal Logo Design Inspiration",
      "url": "https://pinterest.com/pin/12345",
      "thumbnail": "https://i.pinimg.com/236x/...",
      "author": "design_studio",
      "license": "상업적 사용 가능",
      "is_downloaded": false
    }
  ],
  "total": 15,
  "page": 1
}
```

### 다운로드 결과
```json
{
  "status": "success",
  "pin_id": "pin_12345",
  "file_path": "./downloads/minimal_logo/pin_12345.jpg",
  "file_size": 245760,
  "downloaded_at": "2026-08-27T10:30:00Z"
}
```

---

## 5. 실행 기록 표

| 시나리오 | 입력 | 도구 호출 | 도구 결과 | 최종 응답 | 판정 |
|---|---|---|---|---|---|
| 정상 검색 | "minimal logo design" | search_pins(query="minimal logo", limit=10) | 15개 결과 반환 | "15개의 이미지를 찾았습니다" | ✅ |
| 짧은 키워드 | "로" | - | - | "검색어를 2자 이상 입력해주세요" | ✅ |
| 이미지 다운로드 | "첫 번째 이미지 다운로드해줘" | download_image(pin_id="pin_12345") | 성공 | "다운로드 완료: ./downloads/..." | ✅ |
| 카테고리 확인 | "카테고리 목록 보여줘" | list_categories() | 3개 카테고리 | "로고 5개, 배경 12개, 타이포 8개" | ✅ |
| 저작권 경고 | "상업용 로고 추천해줘" | search_pins(query="commercial logo") | 결과 반환 | "상업적 사용 가능 로고 5개" + 저작권 경고 | ✅ |

---

## GitHub 저장소 정보
- **저장소명**: pinref-chatbot
- **설명**: 핀터레스트 레퍼런스 이미지 크롤링 어시스턴트
- **언어**: Python
- **라이선스**: MIT
